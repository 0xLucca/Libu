const nextConfig = {
  reactStrictMode: true,
  swcMinify: true,
  images: {
    domains: ['ipfs.io', 'locksmith.unlock-protocol.com'],
  },
};

module.exports = nextConfig;
/*

      'locksmith.unlock-protocol.com',
      'ipfs.io',
*/
